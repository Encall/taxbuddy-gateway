# Backend host (e.g. localhost:8080 for local dev, or internal IP in prod)
BACKEND_HOST=localhost:8080

# Retriever Tailscale IP and port (local private server connected via Tailscale)
# Run `tailscale ip -4` on the retriever host to get this value
RETRIEVER_HOST=100.x.x.x:8001
